{
  "name": "langgraph-deepen-demo",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "投资档案分拣系统 · 取证 Agent 的 LangGraph 实现（可离线跑）",
  "scripts": {
    "demo": "node --import tsx src/run.ts",
    "demo:budget": "node --import tsx src/run.ts --scenario=budget",
    "demo:real": "node --import tsx src/run.ts --model=gateway",
    "test": "node --import tsx --test src/agent.test.ts",
    "ts-check": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@langchain/core": "^0.3.72",
    "@langchain/langgraph": "^0.4.9",
    "openai": "^7.4.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
