node_modules
dist
.DS_Store
*.swp
.git/*
__pycache__/
*.pyc
.venv/*
*.o
*.a
/vendor
*.egg-info/
/.env
.env

# JS/TS bundlers & build outputs
.next
.nuxt
.turbo
.vercel
.parcel-cache
.cache
coverage
storybook-static

# Package manager artifacts
package-lock.json
yarn.lock
.pnp*
.npm

# Python tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/

# Go tooling
bin/
*.test

# Editor / IDE
.vscode/
.idea/
*.iml

# OS / misc
Thumbs.db
ehthumbs.db

# Logs
output.log
*.log
*.log.*
logs/

# 视频文件
*.mp4
*.avi
*.mov
*.wmv
*.flv
*.mkv
*.webm
*.m4v
*.mpeg
*.mpg
*.3gp
*.f4v
*.rmvb
*.vob

# 归档文件
*.iso
*.dmg
*.rar
*.zip
*.gz

# 文档类也默认加入
*.pdf
*.docx
*.doc
*.xlsx
*.xls
*.ppt
*.pptx
*.xlsx
*.csv
.cozeproj/reduction

# Preview files
.preview
.codegraph
# Dependencies
node_modules/
.pnp
.pnp.js
# Production build
dist/
build/
out/
.next/
.rsbuild/
# Testing
coverage/
*.lcov
.nyc_output
# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# Runtime data
pids
*.pid
*.seed
*.pid.lock
# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
# Cache
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
node-compile-cache/
# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swo
*~
# OS files
.DS_Store?
._*
.Spotlight-V100
.Trashes
Desktop.ini
# Temporary files
*.tmp
*.temp
.tmp/
.temp/
# Optional files
*.tgz
*.tar
# TypeScript
*.tsbuildinfo
# Misc
.coze-logs
.codegraph/

# 客户真实项目档案。
# 君柔、佰特微两份自 2026-08-24 起进版本库——项目负责人声明已脱敏、确认可上传。
# 目的：让评测能在 Coze 环境跑。模型凭证（COZE_INTEGRATION_MODEL_BASE_URL、
# COZE_WORKLOAD_IDENTITY_API_KEY）只注入到 Coze 那边的应用进程，本机取不到，
# 所以评测必须在有凭证的一侧跑，样本得跟过去。放行规则见本文件末尾。
#
# 另两份样本尚未脱敏，继续忽略：
/聚芯DD/
/微量介入/


# 评测结果不进版本库。那份"6/6"曾被当成产品准确率引用（有答案泄漏、且用的不是
# 线上抽取器），结果文件留在仓库里就会继续被引用。谁跑谁看。
/evaluation/reports/
# 评测产物的旧位置，保留规则以防再写回来。
/output/

# 单元测试同样只在本机跑，不在 Coze 平台跑，因此不进版本库。
# 代价要知道：它们是唯一会自动发现"改坏了"的东西（例如打包目录结构那 11 条挡住过
# 压缩包丢项目名的 bug）。忽略之后这层保护只存在于本机，换机器或重新克隆就没有了。
# 跑法：node --import tsx --test tests/*.test.ts
/tests/

# 个人简历素材，不进版本库。
/RESUME_CN.md
/RESUME_EN.md

# ---------------------------------------------------------------------------
# 评测样本放行（必须写在文件末尾）
# ---------------------------------------------------------------------------
# 上面按扩展名的全局规则（*.pdf / *.docx / *.xlsx / *.pptx / *.zip …）会把这两个
# 目录里的文件全部挡掉。git 是后写的规则覆盖先写的，所以放行只能放在最后。
!/君柔档案/**
!/佰特微档案/**
# 放行用的是通配，要把系统垃圾文件重新挡回去。
/君柔档案/**/.DS_Store
/佰特微档案/**/.DS_Store
