# 分类判断层重构方案

状态：待评审｜2026-08-04

## 1. 这次要解决什么

现有系统在 35 份真实文件上的表现（实测，非估算）：

- 关键词表只对 **8/35** 份文件说得出话，其余 27 份直接判定"证据不足"转人工；
- 这张覆盖 8/35 的表，对写得很好的章程比对逻辑拥有**一票否决权**；
- 名为 Agent 的编排层有 5 个节点，**没有一个在做判断**，它对最终得分的贡献上限是 20 分；
- 事实抽取把模型输出的字符串 `"null"` 当成真实文本存下，凭空制造出不存在的交易变化。

一句话：**判断权在覆盖面最窄的组件手里，而真正有能力的组件在给它打下手。**

## 2. 三条设计原则

这三条是为了防止重蹈覆辙。现有系统的每个毛病都能对应到其中一条的违反。

### 原则一：让不完整的东西只做加法，永远不要让它挡路

任何"人手写、覆盖不全"的东西——关键词表、类型映射表、逐类比对规则——只允许出现在两个位置：

- **加速**：它能确定的，跳过后续步骤省一次模型调用；
- **挑刺**：判断做完之后，它发现矛盾就拦下来转人工。

不允许出现在必经之路上。判据很简单：**少写一条规则，后果应该是"少一次优化"，而不是"这份文件判不出来"。**

今天的关键词表正好违反这条——它覆盖 8/35，却卡在所有文件的必经之路上，说不出话就全员摆烂。

### 原则二：歧义靠观察发现，不靠清单预设

程序不可能提前知道下一个项目里哪类文件会重复。任何形如"章程通常有两份、尽调报告通常只有一份"的假设都会在换一个项目时失效。

歧义必须由**运行时能观察到的事实**触发。详见第 3 节。

### 原则三：目标是"默认选项对"，不是"绝不出错"

每份文件都要人工点确认才入库。这意味着：

- 判错的代价是人多点两下，不是合规事故；
- 但**判不出来**的代价是把整个活推还给人——系统等于没干活。

现在系统用"证据不足转人工"来回避判断，27/35 份文件走这条路。这是在放弃它唯一的价值。新方案对每份文件都要给出**最可能的答案 + 凭什么**，把握不足就标出来，但不能沉默。

## 3. 歧义怎么自动识别

这是本方案的核心。**不预设任何文件类型清单**，靠三个运行时信号，任一命中即进入第二轮取证：

### 信号一：项目里已经有同类型文件

把项目记忆里已归档文件按抽取出的类型分组，看当前文件的类型有没有"兄弟"。

- 项目里出现第二份公司章程 → 触发；
- 某个项目有三份不同轮次的尽调报告 → 同样触发，不需要任何人预先知道；
- 项目里只有一份章程 → 不触发，直接判。

**这是纯数据观察，零领域知识。** 换项目、换文件类型，逻辑一样成立。

### 信号二：模型自己报告证据不足

第一次判断时，除了阶段和把握程度，模型还要回答一个问题：**"你够了吗？不够的话缺什么？"**

模型说缺，就按它说的去取。模型对自己认知状态的报告，比任何预设清单都准，而且天然随文件内容变化。

### 信号三：事实里同时出现方向相反的标记

同一份文件的事实里既出现交易前的措辞、又出现交易后的措辞（例如同时有"原股东"和"增资后"），说明这份文件本身跨越了交易节点，需要比对才能定位。

这是通用的文本对照，不绑定具体文件类型。

### 为什么这样就够了

三个信号覆盖了歧义的三种来源：**项目里有可混淆的对象**（信号一）、**模型知道自己不确定**（信号二）、**文件内容自相矛盾**（信号三）。

它们都不是"知识"，是"观察"。这是它和写死清单的根本区别。

## 4. 新的判断流程

```
① 读文件 ────────────────────── 不变（含扫描件 OCR）
        ↓
② 抽事实 ────────────────────── 不变（修 null bug）
        ↓
③ 快速通道判定
   类型明确 + 项目内无同类文件？
        │
        ├─ 是 ──→ 直接按类型定阶段，0 次判断调用
        │
        └─ 否 ↓
④ 第一次判断（模型）
   输入：事实 + 项目上下文 + 已归档同类文件清单（只给标题和类型，不给全文）
   输出：阶段 + 把握程度 + 理由 + 证据 + 「我够了吗 / 还缺什么」
        ↓
   够了？──── 是 ──→ 出结论
        │
        └─ 否 ↓
⑤ 取关联文件 → 第二次判断（模型）
   用现有的关联文件加权评分挑出最相关的 1-2 份，把它们的事实补进去重判。
   第二次判断后不再循环，无论结果如何都出结论。
        ↓
⑥ 规则校验（不调模型）
   逐类比对规则、项目时间线、关键词表全部降级为质检员：
   发现与结论矛盾就强制转人工，并写明矛盾在哪。
   校验器可以缺失——缺失只意味着少一道检查，不影响出结论。
        ↓
⑦ 摆成默认选项，等人工确认 ──── 不变
```

## 5. 模型调用预算

按"1-2 次，越省越好"的要求设计：

| 情况 | 抽事实 | 判断 | 合计 | 占比预估 |
|---|---|---|---|---|
| 类型明确、项目内无同类 | 1 | **0** | **1** | 多数 |
| 需要判断 | 1 | 1 | **2** | 少数 |
| 需要比对 | 1 | 2 | **3** | 极少数 |

对照今天：抽事实 1 次 + 判断 0 次 = 1 次，但代价是 27/35 判不出来。

**新方案的多数情况仍然是 1 次调用**，因为第 ③ 步的快速通道用现成的类型映射直接定阶段。这张映射表覆盖 11 种类型——按原则一，它只用来省调用，覆盖不到的类型自动落到第 ④ 步交给模型，不会因为表里没有就判不出来。

第 ⑤ 步不设循环上限参数，就是固定最多一轮补充。理由：现有 Agent 允许 3 轮，实测真实案例最多用到 1 轮，多出来的轮次是空转。

## 6. 保留与废弃

### 保留（这些是真本事，不要动）

| 组件 | 理由 |
|---|---|
| 文件读取与扫描件 OCR | 六份金标准文件全是无文字层扫描件，这是必经之路 |
| 事实抽取 | 系统最硬的能力，实测能从扫描件读出 11.73624 / 13.04027 这类关键数字 |
| 项目上下文综合 | 时间线是判断历史文件归属的关键依据 |
| 关联文件加权评分 | 有明确关系 +120、同事件 +100、同类型 +40 等，写得好，直接搬进第 ⑤ 步 |
| 5 类文件的逐类比对规则 | **改当校验器**，见下 |
| 归档确认流程、目录结构、存储、留痕 | 完全不动 |

### 降级（从裁判改成质检员）

| 组件 | 今天 | 改成 |
|---|---|---|
| 关键词表 | 决定阶段，且对其他逻辑一票否决 | 只在结论与它明显冲突时提示，不否决 |
| 5 类比对规则 | 决定 5 类文件的归属 | 判断做完后校验，发现矛盾则转人工 |
| 项目时间线引用 | 直接加 70 分 | 作为模型的输入证据 + 校验依据 |

### 废弃

- 关键词表的裁判权与那道 50 分门槛；
- 现有 5 节点编排图（保留其中的关联文件评分，其余重写）；
- 恒为 0 的调度层调用计数，以及界面上那行读起来像测量结果的显示。

## 7. 分期实施

### 一期：止血（不依赖架构改动，建议立刻做）

1. 修 `"null"` 字符串——把 `"null"`、`"无"`、`"N/A"` 归成真正的空值。这个 bug 在污染所有下游判断，不只是模型这一路。
2. 给现有的模型影子判断补两条规则：**项目当前阶段不得作为文件自身阶段的正面证据**；**存在同类型关联文件时必须比对并说明结果**。
3. 加代码护栏：模型自己报告了矛盾时强制转人工，不允许高分直接放行。

做完可以立刻用那两份公司章程验证有没有救回来。

### 二期：判断层重构

按第 4 节实施。改完必须保证：那两份章程仍然判对（现在规则是 2/2，不能退步）。

### 三期：评测修复

1. 评测输入去掉目录前缀——现有 6 个案例里 5 个的路径里直接写着正确答案，评测无法区分"真会判"和"读路径"；
2. 让系统自己生成项目上下文再评测，不再喂手写的完美版本；
3. 金标准从 6 份扩到 35 份（需要业务同事标注）。

## 8. 怎么算成功

| 指标 | 今天 | 目标 |
|---|---|---|
| 能给出阶段建议的比例 | 8/35 | 大幅提升，且不牺牲现有正确项 |
| 两份公司章程 | 规则 2/2 | 保持 2/2 |
| 多数文件的模型调用次数 | 1 | 保持 1 |
| 判错时的把握程度 | 判错比判对还自信（85% vs 80%） | 把握程度与正确率正相关 |

最后一条最关键。系统只要把握程度是可信的，人就知道该重点看哪几份；把握程度不可信，等于每份都得重看，前面做多少都白搭。
