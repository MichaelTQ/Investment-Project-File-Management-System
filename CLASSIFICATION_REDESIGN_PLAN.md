# 分类判断层重构方案

状态：第三版｜2026-08-04

第三版把架构从"上传时尽力判准"改成"上传时给初判 + 每次重建用代码全量校验"。
第一版按"简单/困难"分流（已废弃），第二版加了模型多轮取证（本版取消，被代码校验取代）。
产品定位是**初筛，不取代人工审核**，本版按这个定位大幅削减了机制。

## 1. 这次要解决什么

现有系统在 35 份真实文件上的表现（实测）：

- 关键词表只对 **8/35** 份文件说得出话，其余 27 份直接"证据不足"转人工；
- 这张覆盖 8/35 的表，对写得很好的章程比对逻辑拥有**一票否决权**；
- 名为 Agent 的编排层 5 个节点**没有一个在做判断**，对最终得分的贡献上限 20 分；
- 分类相关代码约 5400 行，其中**项目记忆 + 上下文综合占 2800 行**，而这两块在两次章程实测中贡献的决定性证据是零。

一句话：**判断权在覆盖面最窄的组件手里，而一半以上的代码在支撑初筛用不到的机制。**

## 2. 四条设计原则

**原则一：让不完整的东西只做加法，永远不要让它挡路。**
手写的、覆盖不全的东西只能用来加速或挑刺，不能挡在必经之路上。判据：少写一条规则，后果应该是"少一次优化"，而不是"这份文件判不出来"。

**原则二：歧义靠观察发现，不靠清单预设。**
程序不可能提前知道下个项目里哪类文件会重复。任何"章程通常有两份"的假设换个项目就失效。

**原则三：目标是"默认选项对"，不是"绝不出错"。**
每份文件都要人工确认才入库，判错的代价是多点两下，**判不出来**的代价是把活推还给人。说"判不出来"时必须说清缺什么——"证据不足"是废话，"缺一份记载资本变更的股东会决议"是可执行的信息。

**原则四：人工知识写成"依据"，不写成"结论"。**
写死结论（"缴款通知书 → 投资实施，45 分"）不能泛化；写清依据（"文件资本与交易文件的变更后值一致 → 交易完成后形成"）可以。这不是去掉人工知识，是换个存在形式。

## 3. 分工：事实 / 关系 / 语义

两份公司章程的实测里，模型准确读出 11.73624 和 13.04027，也正确比对出不一样，**然后把方向推反了**——它认出当前文件数字更小，却断定是"增资后版本"。同样两个数字规则解释对了。

**模型缺的不是能力，是方向性语义。**

| 层 | 内容 | 谁做 |
|---|---|---|
| **事实** | 这份文件写了什么 | **模型** |
| **关系** | 这个数字和哪份文件的哪个数字对得上 | **代码** |
| **语义** | 对上"变更前"意味着更早 | **代码，写死** |

模型负责看见，代码负责定先后。

## 4. 两个时机，两种机制（本版核心）

**决定性证据经常比它要决定的那份文件晚到。** 先传的章程当时判不准是正常的——直到股东会决议进来，才能一次性把两份章程都钉死。

所以判断分两个时机：

| 时机 | 做什么 | 谁做 | 成本 |
|---|---|---|---|
| **上传时** | 给出初判，摆成默认选项 | 模型 | 每份 1–2 次调用 |
| **每次重建** | 全量重算可计算的信号，与存着的结论 diff | **代码** | **0** |

重建免费，所以每次上传后都做，不需要挑时机。

### 上传时（简化）

第二版设计过"模型判不准就再取证判一次"。**本版取消**——既然重建时代码会全量兜底，上传时没必要为难模型。

- 类型明确且项目内无同类文件 → 按类型直接定，**0 次判断调用**
- 其余 → 带上项目内相关文件的事实，**1 次判断调用**，不再有第二轮

### 重建时（代码全量 diff）

遍历项目全部文件，重算下列确定性信号，与每份文件存着的结论对比：

**数字链**——项目里任何文件记载了"由 X 变为 Y"（股东会决议、增资协议、减资决议），其余文件的数字匹配 X 还是 Y，直接定出在交易哪一侧。**方向无关，减资同样成立。**

**日期链**——文件日期与已确认交易事件的先后关系。

**重复文件**——哈希相同却归进不同文件夹，必有一错。当前数据集里有 2 组重复文件。

**阶段冲突**——一份文件被判到立项前，却被交割阶段的事件引用。

**档案缺口**——有交割确认函却没有增资协议，提示可能缺文件。**这条查的是归档完整性而非分类对错，是初筛场景最容易被人漏掉的部分。**

### diff 的结果是三种，不是两种

| 代码算出 | 与存着的结论 | 动作 |
|---|---|---|
| 有结论 | 一致 | 不动 |
| 有结论 | **不一致** | **提示用户** |
| **没结论** | — | **不动** |

第三种必须单独处理：**代码沉默 ≠ 代码反对。** 财务报表这类文件代码本来就说不出话，不能因为它没意见就去动已确认的分类。

### 为什么这样成立

代码能**推翻**判断，不总能**产生**判断。给定所有事实，"A 记载 11.73、交易文件写 11.73→13.04、C 记载 13.04，所以 A 在前 C 在后"是纯计算；但"这份财务报表属于尽调还是投资决策"没有数字链可循，代码算不出来——这正是今天 27/35 沉默的原因。

所以代码负责校验，模型负责初判。**文件越多，代码查得越准**；而模型判断的准确率不随文件数提升。两者的规律正好互补。

### 废弃：比大小

不要凭注册资本大小推先后。注册资本不单调递增——减资、股权回购、对赌退出都会让它减少；2024 年新公司法要求五年实缴，2025–2026 年大量公司主动减资。股东数信号更弱，老股转让根本不改变股东数。

**"较小的更早"只在"本次交易是增资"的前提下成立，而这正是待判断的东西。循环论证。**

同一问题存在于现有代码：章程比对里有个 30 分的信号直接比大小，证据文案写死了"更可能是增资后版本"。项目里还没归档交易文件时它是唯一的资本信号——**那时遇上减资就直接判反，必须删除。**

## 5. 把握程度怎么算

实测两次模型都给 85%，一次对一次错，**完全没有区分度**。

按原则三，系统的价值是让人知道该重点看哪几份。把握程度不可信，人就得每份都重看，前面做多少都白搭。**这是所有指标里最关键的一个。**

对策：**不让模型自报，改由证据强度推导**（确定性计算，可解释）：

| 拿到的证据 | 把握 |
|---|---|
| 匹配上交易记录的变更前/后值 | 高 |
| 只有日期先后关系 | 中 |
| 只有文档类型的阶段先验 | 低 |
| 以上都没有 | 很低，转人工并说明缺什么 |

同样符合第 3 节分工：语义归代码。

## 6. 要存什么

每个项目只持久化三样：

1. **每份文件的极简事实**——判断和校验的唯一原料
2. **每份文件当前的分类结论**——diff 的对象
3. **已被用户驳回的提示**——避免同一个提示反复弹

**没有版本号、没有快照生命周期、没有重建历史、没有进程缓存。**

事实必须跨上传留存（章程比对的唯一前提），但它只需要是一张表，不需要是一套带版本和生命周期的记忆系统。

## 7. 提示怎么呈现

diff 出不一致时用**批量入口**，不用逐条提示：

> 本次新增文件影响了 3 份已归档文件的判断，点击查看

理由：用户刚传完一批文件正在这个页面上，一次看完比被 3 条提示打断好；批量界面里能把"为什么变了"（因为新来的股东会决议）说清楚，逐条提示塞不下这个理由。

提示对象是已归档文件，所以界面上必须能直接改，否则提示了也没法处理。

## 8. 保留与废弃

**保留**：文件读取与 OCR、事实抽取、归档确认流程、目录结构、存储、证据留痕。

**降级为校验器**：5 类逐类比对规则（其中的数字链和日期链逻辑搬进代码校验器）。

**废弃**：
- 关键词表的裁判权与 50 分门槛
- **项目上下文综合器**（906 行）——时间线可由代码按日期排序拼出，不需要模型
- **项目记忆的版本快照、revision、重建历史**（约 1900 行）
- 现有 5 节点编排图（保留其中的关联文件挑选逻辑）
- 12 小时进程内缓存
- 章程比对里 30 分的比大小信号
- 恒为 0 的调度层调用计数及其界面显示

**改成只能排除、不能支持**：项目整体进度。"项目最晚阶段是投资实施"对每份文件都给同样答案，不携带区分单份文件的信息——35 份里只有 12 份真属于投资实施，其余 23 份会被拽错。它是上界，不是指向。

**这条必须写进代码。** 一期只在指令里禁止，实测模型完全无视——因为这个信号大多数时候确实"对"。**凡是"禁止模型做某事"的约束，都不能只写在指令里。**

## 9. 分期实施

### 一期：止血 ✅ 已完成（提交 26f9954）

空值字面量还原、模型指令补三条、矛盾护栏写进代码。实测：假交易消失、两次都做了比对、护栏生效，但方向判断仍出错——这正是本版重写的起因。

### 二期：极简化 + 代码全量校验

1. 事实表瘦身，只存第 6 节那三样；
2. 上传时判断简化为最多 1 次判断调用，取消多轮取证；
3. 实现代码全局校验器（数字链、日期链、重复文件、阶段冲突、档案缺口）；
4. 批量修正入口；
5. 把握程度改由证据强度推导；
6. 按第 8 节删除：上下文综合器、记忆版本快照、比大小信号、关键词表裁判权、5 节点编排图。

**净减代码预计 2000 行以上。**

验收标准：两份章程 2/2；把握程度有高低之分；能给出阶段建议的比例大幅提升；第三次传入股东会决议后，代码 diff 能自动指出前两份章程的归属。

### 三期：评测修复

1. 评测输入去掉目录前缀——现有 6 个案例里 5 个的路径里直接写着正确答案；
2. 金标准从 6 份扩到 35 份（需要业务同事标注）。

原第二版的"三期判断权交接"已被二期吸收。

## 10. 怎么算成功

| 指标 | 今天 | 目标 |
|---|---|---|
| **把握程度与正确率正相关** | 判错(85%)不低于判对(85%) | **最关键** |
| 能给出阶段建议的比例 | 8/35 | 大幅提升，不牺牲现有正确项 |
| 两份公司章程 | 规则 2/2，模型 1/2 | 2/2 |
| 多数文件的模型调用次数 | 1 | 保持 1 |
| 说"判不出来"时能说清缺什么 | 只说"证据不足" | 指明缺哪类文件 |
| 分类相关代码行数 | 约 5400 | 3000 左右 |

## 11. 什么时候需要重新分层

- **代码全量校验**——几百份文件都不是问题，纯计算；
- **瓶颈在上传时的判断**——模型判某份文件要带上同项目相关文件的事实。35 份约 1 万字符可以全带，**约 100 份**开始塞不下。

所以分层的触发点是**单个项目接近 100 份文件**，不是现在。在那之前不做挑选策略。
